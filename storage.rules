rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Only allow authenticated users. 🛡️
      // In production, you should restrict by folder (e.g., users/{uid}/...)
      allow read, write: if request.auth != null;
    }
  }
}
