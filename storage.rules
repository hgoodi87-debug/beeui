rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // TEMPORARY: Allow all for debugging unauthenticated issue
      allow read, write: if true;
    }
  }
}
